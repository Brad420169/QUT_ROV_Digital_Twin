# Archived experiments

`legacy_ros_nodes/` preserves the previous standalone controllers and detectors.
These are not installed or launched by the current stack. Use
`ros_nodes/` for active development.
